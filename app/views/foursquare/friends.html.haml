= sitemap "#{current_user.name}'s friends"

- current_user.friends.each do |friend|
  .friend
    .photo
      = image_tag(friend.photo) if friend.photo
    .info
      .name= "#{friend.first_name} #{friend.last_name}"
      .city= friend.home_city
      /= friend.inspect
